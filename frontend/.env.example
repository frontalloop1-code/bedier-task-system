# Only needed for production builds (Netlify, Vercel, etc).
# Leave unset in local dev — Vite proxies /api → http://localhost:4000.
#
# Example after deploying backend to Render:
#   VITE_API_BASE_URL=https://bedier-task-system-api.onrender.com/api

VITE_API_BASE_URL=
